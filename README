sudo docker build -t ansible/django .

sudo docker run -p 80 -p 443 -v `pwd`:/srv/ansible/ --rm -it ansible/django 

ansible-playbook -vvvv --inventory-file=/srv/ansible/ansible/inventory.ini \
   /srv/ansible/ansible/site.yml -c local
