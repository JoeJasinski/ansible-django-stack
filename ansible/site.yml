---
- name: Base states
  hosts: all
  vars:
    - update_apt_cache: yes
  vars_files:
    - env_vars/base.yml
  roles:
    - role: base
    - role: unattended
    - role: ntp
    - role: sites
    - role: supervisor
    - role: nginx
    - role: django
