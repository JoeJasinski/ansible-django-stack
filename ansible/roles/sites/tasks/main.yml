---
- name: Creates directory
  file:
    path={{ sites_site_dir }}
    state=directory
    owner=www-data
    group=worker
    mode=2775
    recurse=yes
  tags:
    - sites

- include: setup_virtualenv.yml
  tags:
    - virtualenv
    - sites

- include: setup_project.yml
  tags:
    - project
    - sites
